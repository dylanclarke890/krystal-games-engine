export class TileSelector {
  constructor(layer, ctx, scale) {
    this.layer = layer;
    this.ctx = ctx;
    this.scale = scale;
  }

  startSelection() {}
  getSelection() {}
  endSelection() {}
}
