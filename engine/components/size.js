export class Size {
  /** @type {number} */
  x;
  /** @type {number} */
  y;

  /**
   * @param {number} x width
   * @param {number} y height
   */
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}
