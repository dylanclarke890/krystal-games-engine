export class ImageScaler {
  /**
   * @param {Image} image
   * @param {number} scale
   */
  static resizeImage(image, scale) {}

  /**
   * @param {Image} image
   * @param {number} x
   * @param {number} y
   * @param {number} width
   * @param {number} height
   */
  static getImagePixels(image, x, y, width, height) {}
}
